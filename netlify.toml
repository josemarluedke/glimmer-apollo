[build]
  command = "pnpm install && pnpm --filter site build"
  publish = "site/dist"

[build.environment]
  NODE_VERSION = "25"
  PNPM_VERSION = "11.1.2"
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
