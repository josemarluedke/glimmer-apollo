[build]
  command = "npx --yes pnpm@11 install && npx --yes pnpm@11 --filter site build"
  publish = "site/dist"

[build.environment]
  NODE_VERSION = "25"
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
